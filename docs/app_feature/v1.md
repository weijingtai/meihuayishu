# 梅花易数 - 旧版 UI 功能详细清单

> 基于 `example/lib/main.dart` (2274行) 分析

---

## 一、整体架构

### 1.1 页面结构

```
MyApp (MaterialApp)
└── HomePage
    ├── AppBar
    │   ├── Title: '梅花易数'
    │   ├── SettingsButton (主题设置)
    │   └── TabBar (4个Tab)
    │       ├── Tab 1: 时空 (access_time)
    │       ├── Tab 2: 报数 (numbers)
    │       ├── Tab 3: 文字 (text_fields)
    │       └── Tab 4: 手动 (touch_app)
    └── TabBarView
        ├── _buildTimeTab()
        ├── _buildNumberTab()
        ├── _buildTextTab()
        └── _buildManualTab()
```

### 1.2 状态管理

| 状态变量 | 类型 | 默认值 | 说明 |
|----------|------|--------|------|
| `_result` | `DivinationResult?` | `null` | 起卦结果 |
| `_selectedUpperGua` | `int?` | `null` | 上卦选择 |
| `_selectedLowerGua` | `int?` | `null` | 下卦选择 |
| `_selectedYao` | `int?` | `null` | 动爻选择 |
| `_isXianTianGua` | `bool` | `true` | 先天/后天卦模式 |
| `_inputNumber` | `String` | `''` | 报数输入 |
| `_inputText` | `String` | `''` | 文字输入 |
| `_selectedTextMethod` | `TextDivinationMethod` | `byStroke` | 文字起卦方式 |
| `_textAnalysisSummary` | `TextAnalysisSummary?` | `null` | 文字分析结果 |
| `_isTextLoading` | `bool` | `false` | 文字加载状态 |
| `_toneOverrides` | `Map<String, int>` | `{}` | 音调修改映射 |
| `_longTextThreshold` | `int` | `10` | 长文本阈值 |
| `_sentenceCountOverrides` | `Map<int, int>` | `{}` | 句子字数修改映射 |
| `_showFlow` | `bool` | `false` | 流程显示状态 |
| `_isLunarDivination` | `bool` | `false` | 农历起卦标记 |

---

## 二、AppBar 功能

### 2.1 SettingsButton (主题设置按钮)

**文件**: `lib/widgets/settings_button.dart` (36行)

**功能**:
- 点击打开 `ThemeEditPage`
- 传递当前配置 `currentConfig`
- 回调 `onConfigChanged` 更新配置

**调用位置**: `main.dart:88-94`

### 2.2 TabBar

**标签定义**:
| Tab | 图标 | 文字 | 对应页面 |
|-----|------|------|----------|
| 时空 | `access_time` | '时空' | `_buildTimeTab()` |
| 报数 | `numbers` | '报数' | `_buildNumberTab()` |
| 文字 | `text_fields` | '文字' | `_buildTextTab()` |
| 手动 | `touch_app` | '手动' | `_buildManualTab()` |

---

## 三、Tab 1: 时空起卦 (119-402行)

### 3.1 当前时间起卦卡片

**UI组件**:
```
┌─────────────────────────────────────┐
│  ⏰ 图标 (48px, deepPurple)         │
│  '当前时间起卦' (24px, bold)        │
│  '根据当前时间自动起卦...'          │
│  ┌─────────────────────────────┐    │
│  │ 2024年3月23日               │    │
│  │ 14:30:25 (20px, bold)       │    │
│  └─────────────────────────────┘    │
│  [一键起卦] [一键农历]              │
└─────────────────────────────────────┘
```

**时间显示** (190-210行):
- 格式: `${now.year}年${now.month}月${now.day}日`
- 时间: `${HH}:${MM}:${SS}`

**一键起卦按钮** (216-226行):
- 图标: `auto_awesome`
- 文字: '一键起卦'
- 方法: `_generateGuaByTime()`
- 算法: `xianTianDivination([年, 月, 日, 时, 分])`

**一键农历按钮** (228-242行):
- 图标: `brightness_3`
- 文字: '一键农历'
- 颜色: 橙色背景，白色文字
- 方法: `_generateGuaByLunarTime()`
- 算法:
  1. 使用 `Solar.fromYmd()` 转换阳历
  2. 获取农历年地支序数
  3. 获取农历月
  4. 获取农历日
  5. 获取时辰地支序数
  6. 调用 `timeDivination()`

### 3.2 农历手动起卦卡片

**UI组件**:
```
┌─────────────────────────────────────┐
│  📅 图标 (48px, orange)             │
│  '农历起卦' (24px, bold)            │
│  '输入农历年月日时起卦...'          │
│  ┌─────────────────────────────┐    │
│  │ 年（地支） [下拉选择器]      │    │
│  │ 月（农历） [下拉选择器]      │    │
│  │ 日（农历） [下拉选择器]      │    │
│  │ 时（地支） [下拉选择器]      │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 起卦公式：                  │    │
│  │ 上卦 = (年 + 月 + 日) ÷ 8  │    │
│  │ 下卦 = (年+月+日+时) ÷ 8   │    │
│  │ 动爻 = (年+月+日+时) ÷ 6   │    │
│  └─────────────────────────────┘    │
│  [农历起卦]                         │
└─────────────────────────────────────┘
```

**下拉选择器** (278-439行):
- 构建方法: `_buildLunarSelector()`
- 参数:
  - `label`: 标签文字
  - `items`: 选项列表
  - `onChanged`: 值改变回调

| 选择器 | 范围 | 选项 |
|--------|------|------|
| 年（地支） | 1-12 | 子1, 丑2, ..., 亥12 |
| 月（农历） | 1-12 | 1月, 2月, ..., 12月 |
| 日（农历） | 1-30 | 初1, 初2, ..., 30 |
| 时（地支） | 1-12 | 子1, 丑2, ..., 亥12 |

**公式说明** (332-375行):
- 背景: 橙色浅色
- 边框: 橙色
- 内容:
  - 起卦公式（加粗）
  - 上卦公式
  - 下卦公式
  - 动爻公式

**农历起卦按钮** (378-390行):
- 图标: `auto_awesome`
- 文字: '农历起卦'
- 颜色: 橙色背景，白色文字
- 方法: `generateLunarGua()`
- 算法: `timeDivination(yearZhiNum, lunarMonth, lunarDay, hourZhiNum)`

---

## 四、Tab 2: 报数起卦 (442-545行)

### 4.1 界面布局

```
┌─────────────────────────────────────┐
│  🔢 图标 (48px, deepPurple)         │
│  '报数起卦' (24px, bold)            │
│  '输入数字起卦...'                  │
│  ┌─────────────────────────────┐    │
│  │ '输入数字' (12px, grey)     │    │
│  │ 请输入... (28px, grey)      │    │
│  └─────────────────────────────┘    │
│  ┌─────┬─────┬─────┐                │
│  │  1  │  2  │  3  │                │
│  ├─────┼─────┼─────┤                │
│  │  4  │  5  │  6  │                │
│  ├─────┼─────┼─────┤                │
│  │  7  │  8  │  9  │                │
│  ├─────┼─────┼─────┤                │
│  │ 清除│  0  │ 删除│                │
│  └─────┴─────┴─────┘                │
│  [起卦]                             │
└─────────────────────────────────────┘
```

### 4.2 数字输入显示 (468-496行)

- 宽度: 全宽
- 内边距: 16px
- 背景: 灰色浅色
- 圆角: 12px
- 边框: 灰色
- 显示内容:
  - 标签: '输入数字' (12px, grey)
  - 值: `_inputNumber` 或 '请输入...' (28px, bold)
  - 空值时显示灰色，有值时显示黑色

### 4.3 数字键盘 (498-522行)

**布局**:
- 网格: 3列
- 宽高比: 1.5
- 间距: 8px

**按钮列表**:
| 位置 | 按钮 | 功能 |
|------|------|------|
| 1-9 | 数字按钮 | 输入数字 |
| 10 | '清除' (橙色) | 清空输入 |
| 11 | '0' | 输入0 |
| 12 | '删除' (红色) | 删除最后一位 |

**数字按钮** (548-563行):
- 背景: 白色
- 文字: 黑色
- 圆角: 8px
- 字号: 24px, bold
- 点击: 追加数字到 `_inputNumber`

**操作按钮** (565-575行):
- 背景: 颜色浅色 (alpha 0.1)
- 文字: 颜色
- 圆角: 8px
- 字号: 16px

### 4.4 起卦按钮 (524-536行)

- 图标: `casino`
- 文字: '起卦'
- 启用条件: `_inputNumber.isNotEmpty`
- 方法: `_generateGuaByNumber()`
- 算法:
  1. 将字符串转为数字数组
  2. 调用 `xianTianDivination(numbers)`

---

## 五、Tab 3: 文字起卦 (577-826行)

### 5.1 界面布局

```
┌─────────────────────────────────────┐
│  📝 图标 (48px, deepPurple)         │
│  '文字起卦' (24px, bold)            │
│  '输入文字，选择起卦方式...'        │
│  长文本阈值: [10] 字                │
│  ┌─────────────────────────────┐    │
│  │ 输入文字 [TextField]        │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  起卦方式 (Radio选择)               │
│  ○ 按笔画起卦                       │
│  ○ 按现代四声起卦                   │
│  ○ 按古代平仄起卦                   │
│  ○ 按句子字数起卦                   │
├─────────────────────────────────────┤
│  字符分析卡片 (短文本)              │
│  或 句子分析卡片 (长文本)           │
├─────────────────────────────────────┤
│  [起卦 / 长文本起卦]                │
└─────────────────────────────────────┘
```

### 5.2 长文本阈值设置 (636-669行)

**组件**:
- 标签: '长文本阈值: '
- 输入框: 60px宽，数字键盘
- 后缀: ' 字'
- 默认值: 10
- 存储: `_longTextThreshold`

**逻辑**:
- 当输入文字的非标点字符数 > `_longTextThreshold` 时，切换到长文本模式
- 长文本模式下显示句子分析，跳过逐字分析

### 5.3 文字输入框 (671-715行)

**属性**:
- 行数: 短文本1行，长文本4行
- 标签: '输入文字'
- 提示: 短文本 '请输入汉字'，长文本 '请输入长文本...'
- 前缀图标: `edit`
- 圆角: 12px

**onChange逻辑**:
1. 更新 `_inputText`
2. 设置 `_isTextLoading = true`
3. 清空 `_toneOverrides`
4. 如果输入非空:
   - 长文本模式: 跳过字符分析
   - 短文本模式: 调用 `calculator.analyzeText(value)`
5. 如果输入为空: 清空分析结果

### 5.4 起卦方式选择 (722-784行)

**显示条件**: `_inputText.isNotEmpty`

**Radio选项**:
| 方式 | displayName | description | isLongTextRecommended |
|------|-------------|-------------|----------------------|
| `byStroke` | 按笔画起卦 | 根据每个字的繁体笔画数起卦 | false |
| `byModernTone` | 按现代四声起卦 | 根据现代汉语四声（1-4声）起卦 | false |
| `byAncientTone` | 按古代平仄起卦 | 根据古代平仄（平声/仄声）起卦 | false |
| `bySentenceLength` | 按句子字数起卦 | 统计每句字数，按字数起卦 | true |

**长文本推荐标记**:
- 条件: `isLong && isRecommended`
- 样式: 橙色背景小标签 '推荐'

### 5.5 字符分析卡片 (短文本模式) (952-1118行)

**显示条件**: `!isLong && _textAnalysisSummary != null && !_isTextLoading`

**布局**:
```
┌─────────────────────────────────────┐
│  字符分析 (16px, bold)              │
├─────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐           │
│  │  我 │ │  爱 │ │  你 │           │
│  │ 7画 │ │ 10画│ │ 6画│           │
│  │ wǒ │ │ ài │ │ nǐ │           │
│  │ [3] │ │ [4] │ │ [3]│           │
│  │ 仄 │ │ 仄 │ │ 仄 │           │
│  └─────┘ └─────┘ └─────┘           │
├─────────────────────────────────────┤
│  总笔画: 23  平声: 0声  仄声: 3声  │
└─────────────────────────────────────┘
```

**单字卡片** (1002-1098行):
- 宽度: 80px
- 内边距: 8px
- 背景: 灰色浅色
- 圆角: 8px
- 边框: 灰色

**内容**:
1. **汉字** (20px, bold)
2. **笔画数** (12px, grey) - 格式: `${N}画`
3. **拼音和音调** (Row):
   - 拼音 (12px, blue) - 带声调标记
   - 音调按钮 (可点击):
     - 样式: 蓝色背景，圆角4px
     - 点击: 音调在1-4之间循环
     - 更新: `_toneOverrides[character]`
4. **平仄标签**:
   - 平声: 蓝色背景，'平' 字
   - 仄声: 红色背景，'仄' 字
   - 判断: 1-2声为平，3-4声为仄

**统计信息** (1106-1113行):
- 布局: Row，spaceAround
- 项目:
  - 总笔画: `summary.totalStrokes`
  - 平声: `summary.pingCount`声
  - 仄声: `summary.zeCount`声

### 5.6 句子分析卡片 (长文本模式) (828-950行)

**显示条件**: `isLong && !_isTextLoading && sentenceAnalysis != null`

**布局**:
```
┌─────────────────────────────────────┐
│  句子分析           共 X 字         │
│  共 Y 句                            │
├─────────────────────────────────────┤
│  · 第一句内容...            [  8]  │
│  · 第二句内容...            [ 12]  │
│  · 第三句内容...            [  6]  │
└─────────────────────────────────────┘
```

**内容**:
1. **标题**: '句子分析' (16px, bold)
2. **总字数**: '共 $totalChars 字' (12px, grey)
3. **句子数**: '共 $sentenceCount 句' (14px)
4. **句子列表**:
   - 每句一行
   - 项目符号: '· '
   - 句子内容 (maxLines: 2, overflow: ellipsis)
   - **可编辑字数输入框**:
     - 宽度: 50px
     - 高度: 32px
     - 数字键盘
     - 修改后变橙色
     - 更新: `_sentenceCountOverrides[index]`

**字数修改逻辑**:
- 原始字数: `charCounts[index]`
- 当前字数: `_sentenceCountOverrides[index] ?? originalCount`
- 修改标记: `isModified = _sentenceCountOverrides.containsKey(index)`
- 修改后背景: 橙色浅色，边框橙色
- 未修改背景: 白色，边框蓝色

### 5.7 起卦按钮 (805-819行)

- 显示条件: `_inputText.isNotEmpty && !_isTextLoading`
- 图标: `auto_awesome`
- 文字: 长文本 '长文本起卦'，短文本 '起卦'
- 方法: `_generateGuaByTextWithMethod(_selectedTextMethod)`

**起卦逻辑** (2100-2190行):

1. **音调覆盖处理**:
   - 条件: `_toneOverrides.isNotEmpty && (byModernTone || byAncientTone)`
   - 现代音调: 调用 `calculateByModernToneWithOverrides()`
   - 古代平仄: 手动计算平仄值

2. **句子字数覆盖处理**:
   - 条件: `bySentenceLength && _sentenceCountOverrides.isNotEmpty`
   - 调用: `calculateBySentenceLengthWithOverrides()`

3. **默认处理**:
   - 调用: `calculator.calculate(_inputText, method)`

---

## 六、Tab 4: 手动起卦 (1139-1283行)

### 6.1 界面布局

```
┌─────────────────────────────────────┐
│  👆 图标 (48px, deepPurple)         │
│  '手动起卦' (24px, bold)            │
│  '手动选择上下卦和动爻...'          │
├─────────────────────────────────────┤
│  先天卦 / 后天卦 [Switch]           │
│  乾1兑2离3震4巽5坎6艮7坤8          │
├─────────────────────────────────────┤
│  选择上卦 [GuaSelectorWidget]       │
├─────────────────────────────────────┤
│  选择下卦 [GuaSelectorWidget]       │
├─────────────────────────────────────┤
│  选择动爻                           │
│  ┌─────────────────────────────┐    │
│  │ 第6爻 ──────────── (阳)    │    │
│  │ 第5爻 ─ ─ ─ ─ ─ ─ (阴)    │    │
│  │ 第4爻 ──────────── (阳)    │    │
│  │─────────────────────────────│    │
│  │ 第3爻 ──────────── (阳)    │    │
│  │ 第2爻 ─ ─ ─ ─ ─ ─ (阴)    │    │
│  │ 第1爻 ──────────── (阳)    │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  [起卦]                             │
└─────────────────────────────────────┘
```

### 6.2 先天/后天卦切换 (1186-1226行)

**UI组件**:
- 左侧:
  - 模式名称: '先天卦' 或 '后天卦' (16px, bold)
  - 说明:
    - 先天: '乾1兑2离3震4巽5坎6艮7坤8'
    - 后天: '离南坎北震东兑西'
- 右侧: Switch 开关

**切换逻辑**:
- 更新: `_isXianTianGua`
- 重置: `_selectedUpperGua`, `_selectedLowerGua`, `_selectedYao`

**卦数映射**:

先天卦 (1286-1305行):
```
1 → 乾
2 → 兑
3 → 离
4 → 震
5 → 巽
6 → 坎
7 → 艮
8 → 坤
```

后天卦:
```
1 → 坎
2 → 坤
3 → 震
4 → 巽
6 → 乾
7 → 兑
8 → 艮
9 → 离
```

### 6.3 GuaSelectorWidget (卦选择器)

**文件**: `lib/widgets/gua_selector_widget.dart`

**属性**:
- `title`: 标题
- `selectedGua`: 当前选择
- `isXianTian`: 是否先天卦
- `onGuaSelected`: 选择回调

### 6.4 动爻选择器 (1330-1523行)

**构建方法**: `_buildYaoSelector()`

**显示条件**: 上下卦都已选择

**六爻显示** (1397-1448行):
- 上卦: 6、5、4爻 (从上往下)
- 下卦: 3、2、1爻 (从上往下)
- 阴爻: 断线 (两段)
- 阳爻: 实线 (一段)

**爻按钮** (1450-1484行):
- 布局: Row
- 左侧: '第N爻' 文字
- 右侧: 爻形状
- 选中样式:
  - 背景: deepPurple浅色
  - 边框: deepPurple, 2px
  - 文字: deepPurple, bold
- 未选中样式:
  - 背景: 透明
  - 边框: 灰色, 1px
  - 文字: 黑色

**爻形状** (1486-1523行):
- 阳爻: Container (40x6, 黑色, 圆角3)
- 阴爻: Row
  - 左段: Container (16x6, 黑色, 圆角3)
  - 间距: 8px
  - 右段: Container (16x6, 黑色, 圆角3)

### 6.5 起卦按钮 (1265-1276行)

- 启用条件: `_canGenerateManual`
- 条件: `_selectedUpperGua != null && _selectedLowerGua != null && _selectedYao != null`
- 方法: `_generateManualGua()`

**起卦逻辑** (2192-2223行):
1. 如果是后天卦模式，转换为先天卦数字
2. 调用 `manualDivination(upperNum, lowerNum, changingYao)`

**后天→先天映射**:
```dart
const houTianToXianTian = {
  1: 6,  // 坎
  2: 8,  // 坤
  3: 4,  // 震
  4: 5,  // 巽
  6: 1,  // 乾
  7: 2,  // 兑
  8: 7,  // 艮
  9: 3,  // 离
};
```

---

## 七、结果展示区域 (1530-1657行)

### 7.1 卦象结果卡片

**布局**:
```
┌─────────────────────────────────────┐
│  卦象结果          [颜色模式切换]   │
├─────────────────────────────────────┤
│    本卦       互卦       变卦       │
│   ┌─────┐   ┌─────┐   ┌─────┐      │
│   │ 天 │   │ 火 │   │ 泽 │      │
│   │─────│   │─────│   │─────│      │
│   │ 地 │   │ 水 │   │ 山 │      │
│   └─────┘   └─────┘   └─────┘      │
├─────────────────────────────────────┤
│  起卦方式: 时间起卦                 │
│  动爻位置: 第3爻                    │
│  本卦: 天地否                       │
│  变卦: 泽山咸                       │
├─────────────────────────────────────┤
│  [显示起卦流程]                     │
└─────────────────────────────────────┘
```

### 7.2 颜色模式切换器 (1734-1786行)

**UI组件**:
- 触发器: Container
  - 背景: deepPurple浅色
  - 圆角: 16px
  - 内容: Row
    - 图标: `palette` (16px, deepPurple)
    - 文字: 当前模式名称 (12px, deepPurple)
    - 箭头: `arrow_drop_down`
- 弹出菜单: PopupMenuButton

**菜单选项**:
| 模式 | displayName | 说明 |
|------|-------------|------|
| `solid` | 纯色模式 | 所有爻使用同一颜色 |
| `bw` | 黑白模式 | 阳爻黑色，阴爻白色 |
| `yinyang` | 阴阳模式 | 阳爻红色，阴爻蓝色 |
| `colorful` | 彩色模式 | 按五行属性显示不同颜色 |

### 7.3 卦象列显示 (1811-1873行)

**每列内容**:
- 标题: '本卦'/'互卦'/'变卦' (14px, deepPurple)
- 卦名: 全名 (28px, bold)
- 五行属性: (仅彩色模式)
  - 格式: `${上卦五行}·${下卦五行}`
- 上卦三爻
- 下卦三爻

**HoverableContainer** (2226-2274行):
- 鼠标悬停效果
- 背景: 透明 → 白色
- 边框: 透明 → deepPurple浅色
- 阴影: 无 → deepPurple (alpha 0.15)
- 动画时长: 200ms

### 7.4 五行颜色显示 (1843-1847行)

**显示条件**: `themeConfig.mode == YaoColorMode.colorful`

**五行对应**:
| 五行 | 对应卦 | 默认颜色 |
|------|--------|----------|
| 金 | 乾、兑 | 金色 |
| 木 | 震、巽 | 绿色 |
| 水 | 坎 | 蓝色 |
| 火 | 离 | 红色 |
| 土 | 艮、坤 | 棕色 |

### 7.5 变爻指示器 (1990-2006行)

**显示条件**: `showIndicator && isChanging && indicator.type != none`

**类型**:
| 类型 | 显示内容 |
|------|----------|
| `none` | 不显示 |
| `text` | 自定义文本 (如 '⭕️') |
| `image` | 星形图标 (star) |

**指示器样式**:
- 文本颜色: `indicator.textColor`
- 字号/大小: `indicator.size`
- 位置: 爻左侧

### 7.6 信息行 (2031-2042行)

**格式**:
```
标签: 值
```
- 标签: 灰色
- 值: 黑色, bold

### 7.7 起卦流程按钮 (1603-1648行)

**UI组件**:
- 触发器: Container
- 背景: 展开时 deepPurple浅色，折叠时 灰色浅色
- 圆角: 20px
- 边框: 展开时 deepPurple，折叠时 灰色
- 内容: Row
  - 图标: `expand_less` (展开) / `expand_more` (折叠)
  - 文字: '显示起卦流程' (13px)
- 点击: 切换 `_showFlow`

---

## 八、起卦流程展示 (1659-1731行)

### 8.1 流程组件

**文件**: `lib/widgets/divination_flow_display.dart` (872行)

**DivinationFlowDisplay**:
- 属性:
  - `methodName`: 方法名称
  - `steps`: 步骤列表
  - `finalResult`: 最终结果
- 布局:
  - 方法名称 (Row)
  - 分割线
  - 步骤列表
  - 最终结果 (如有)

### 8.2 步骤显示

**DivinationFlowStep**:
- `title`: 步骤标题
- `description`: 步骤描述
- `variables`: 变量映射
- `formula`: 公式

**UI组件**:
```
┌─────────────────────────────────────┐
│  [1] 步骤标题                       │
│       步骤描述                      │
│       ┌─────────────────────────┐   │
│       │ 变量1: 值1              │   │
│       │ 变量2: 值2              │   │
│       └─────────────────────────┘   │
│       ┌─────────────────────────┐   │
│       │ ⚙️ 公式内容             │   │
│       └─────────────────────────┘   │
└─────────────────────────────────────┘
```

### 8.3 流程类型

**DivinationFlowFactory** 提供以下流程:

| 方法 | 说明 | 行号 |
|------|------|------|
| `lunarDivinationFlow()` | 农历起卦流程 | 310-399 |
| `timeDivinationFlow()` | 时间起卦流程 | 402-461 |
| `numberDivinationFlow()` | 报数起卦流程 | 464-530 |
| `charCountFlow()` | 字数起卦流程 | 533-589 |
| `strokeFlow()` | 笔画起卦流程 | 592-658 |
| `toneFlow()` | 音调起卦流程 | 661-721 |
| `sentenceCountFlow()` | 句数起卦流程 | 724-771 |
| `sentenceLengthFlow()` | 句子字数流程 | 774-840 |
| `manualFlow()` | 手动起卦流程 | 843-871 |

---

## 九、主题编辑页面 (ThemeEditPage)

**文件**: `lib/pages/theme_edit_page.dart` (561行)

### 9.1 页面结构

```
AppBar
├── Title: '卦爻主题设置'
└── Actions: [重置按钮]

Body
├── 颜色模式选择器
├── 模式配置区域
├── 变爻指示器配置
└── 预览区域
```

### 9.2 颜色模式选择 (72-109行)

- 组件: Wrap + ChoiceChip
- 选项: `YaoColorMode.values`
- 显示: 模式名称
- 选中: 高亮
- 描述: 模式说明 (12px, grey)

### 9.3 模式配置 (131-141行)

**纯色模式** (144-167行):
- 配置项: 爻颜色
- 组件: 颜色选择器

**阴阳色模式** (170-201行):
- 配置项:
  - 阳爻颜色
  - 阴爻颜色
- 组件: 两个颜色选择器

**彩色模式** (204-235行):
- 配置项: 五行颜色
  - 金 (乾、兑)
  - 木 (震、巽)
  - 水 (坎)
  - 火 (离)
  - 土 (艮、坤)
- 组件: 五个颜色选择器

### 9.4 变爻指示器配置 (254-362行)

**类型选择**:
- 组件: Wrap + ChoiceChip
- 选项:
  - `none`: 无
  - `text`: 文本
  - `image`: 图片

**文本配置**:
- 输入框: 指示器文本 (如 '⭕️')
- 颜色选择器: 文本颜色

**图片配置**:
- 显示: 图片路径 (开发中)
- 图标: `image`

**大小配置**:
- 组件: Slider
- 范围: 8-32
- 分段: 24
- 标签: 当前值

### 9.5 预览区域 (421-445行)

**预览卦象**:
- 乾 (阳阳阳), 变爻: 2
- 坤 (阴阴阴), 变爻: 1
- 离 (阴阳阳), 变爻: 3

**每卦显示**:
- 卦名 (bold)
- 三爻 (从上往下)
- 变爻标记

---

## 十、数据模型

### 10.1 YaoColorMode

**枚举值**:
| 值 | displayName | description |
|----|-------------|-------------|
| `solid` | 纯色模式 | 所有爻使用统一颜色 |
| `bw` | 黑白模式 | 阳爻深色，阴爻浅色 |
| `yinyang` | 阴阳模式 | 阳爻红色，阴爻蓝色 |
| `colorful` | 彩色模式 | 按五行属性显示不同颜色 |

### 10.2 YaoThemeConfig

**属性**:
- `mode`: YaoColorMode
- `solidColor`: Color (纯色模式)
- `yangColor`: Color (阳爻颜色)
- `yinColor`: Color (阴爻颜色)
- `wuXingColors`: Map<WuXing, Color> (五行颜色)
- `changingIndicator`: ChangingYaoIndicator

**预设主题**:
- `defaultTheme`: 纯色模式
- `bwTheme`: 黑白模式
- `yinyangTheme`: 阴阳模式
- `colorfulTheme`: 彩色模式

### 10.3 ChangingYaoIndicator

**属性**:
- `type`: ChangingYaoIndicatorType
- `text`: String
- `textColor`: Color
- `imagePath`: String?
- `size`: double

**类型**:
| 类型 | displayName |
|------|-------------|
| `none` | 无 |
| `text` | 文本 |
| `image` | 图片 |

### 10.4 TextDivinationMethod

**枚举值**:
| 值 | displayName | description | isLongTextRecommended |
|----|-------------|-------------|----------------------|
| `byStroke` | 按笔画起卦 | 根据每个字的繁体笔画数起卦 | false |
| `byModernTone` | 按现代四声起卦 | 根据现代汉语四声起卦 | false |
| `byAncientTone` | 按古代平仄起卦 | 根据古代平仄起卦 | false |
| `bySentenceLength` | 按句子字数起卦 | 统计每句字数起卦 | true |

---

## 十一、依赖关系

### 11.1 外部依赖

| 包 | 用途 |
|----|------|
| `flutter/material.dart` | UI组件 |
| `provider` | 状态管理 |
| `lunar` | 农历转换 |

### 11.2 内部依赖

| 模块 | 用途 |
|------|------|
| `MeiHuaService` | 起卦计算 |
| `TextDivinationCalculator` | 文字起卦计算 |
| `DivinationFlowFactory` | 流程显示 |
| `GuaSelectorWidget` | 卦选择器 |
| `SettingsButton` | 设置按钮 |
| `ThemeEditPage` | 主题编辑页 |
| `PinyinToneConverter` | 拼音转换 |

---

## 十二、功能统计

### 12.1 起卦方式

| 方式 | 子类型 | 数量 |
|------|--------|------|
| 时空起卦 | 阳历、农历 | 2 |
| 报数起卦 | - | 1 |
| 文字起卦 | 笔画、音调、平仄、字数 | 4 |
| 手动起卦 | 先天、后天 | 2 |
| **总计** | | **9** |

### 12.2 UI组件

| 组件 | 数量 |
|------|------|
| Tab | 4 |
| Card | 10+ |
| Button | 15+ |
| TextField | 5+ |
| DropdownButton | 4 |
| Switch | 1 |
| RadioListTile | 4 |
| ChoiceChip | 5+ |
| PopupMenuButton | 1 |
| Slider | 1 |

### 12.3 状态变量

| 类型 | 数量 |
|------|------|
| String | 2 |
| int | 3 |
| bool | 4 |
| Map | 2 |
| Object | 3 |
| **总计** | **14** |

---

## 十三、待完善功能

根据代码注释，以下功能标记为"开发中":

1. **图片指示器**: `theme_edit_page.dart:326` - '图片指示器功能开发中'
2. **历史记录**: 无
3. **保存功能**: 无
4. **导出功能**: 无
